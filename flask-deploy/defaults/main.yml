app_base_path: /opt/flask-app
app_user: "{{ ansible_user | default(ansible_user_id) }}"
app_group: "{{ app_user }}"
venv_path: "{{ app_base_path }}/venv"
app_module: "app:app"
app_bind: "0.0.0.0:8000"
app_workers: 2